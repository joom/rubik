; Crane is a submodule built in place, so the extraction plugin never has to be
; installed over the user's opam switch. "build" is the CMake tree.
(vendored_dirs crane)
(dirs :standard \ build html assets)
